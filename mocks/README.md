# Mocks

Mockoon collections for every external service. Run standalone locally via `mockoon-cli` (not orchestrated by this repo); ECS Fargate for AWS demo hosting if the stack runs from cloud infra instead of a laptop (see docs/DECISIONS.md). Populated starting E0.1 T2, full coverage in E8.1 T1.
